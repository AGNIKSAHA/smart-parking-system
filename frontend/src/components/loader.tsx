export const Loader = ({ label }: { label: string }) => (
  <div className="flex min-h-[280px] items-center justify-center">
    <div className="rounded-xl border border-slate-200 bg-white px-5 py-3 text-sm">{label}</div>
  </div>
);
